kestra:
  repository:
    type: memory
  queue:
    type: memory
  worker:
    controllers:
      type: STATIC
      static:
        endpoints:
          - host: localhost
  storage:
    type: local
    local:
      base-path: /tmp/unittest
